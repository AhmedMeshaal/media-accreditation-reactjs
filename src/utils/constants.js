const API_URL = 'http://macc.localhost/api'
export const API_ROUTES = {
    REGISTER: `${API_URL}/register`,
    LOGIN: `${API_URL}/login`,
    GET_USER: `${API_URL}/profile`,
}

export const APP_ROUTES = {
    REGISTER: '/register',
    LOGIN: '/login',
    DASHBOARD: '/home',
}